# Changelog

## 0.1.1

- `win_notify` accepts `mode`: `toast` | `messagebox` (default `messagebox`).
- `toast` uses a non-blocking NotifyIcon BalloonTip; on failure falls back to MessageBox.

## 0.1.0

- Initial public release of `dsh-wsl-notify` for DeepSeek Harness on Windows + WSL.
